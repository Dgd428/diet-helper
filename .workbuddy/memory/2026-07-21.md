# 2026-07-21 工作日志

## Bug 修复
- 修复 `currentMeal` 变量在 `renderMealsContent` 重构中丢失导致全天菜单不渲染
- 修复端口 8080 被多个旧服务进程占用，改用 8088
- 版本号升级至 v3，自动清理旧 localStorage 数据
- 增强 init() 错误处理和调试日志
- JSON 数据内嵌至 HTML（146KB → 150KB），支持双击直接打开无需服务器

## 新功能
- **全天预览**：当前餐下方折叠显示其余两餐，点击展开
- **采购清单**：手动入口在「全天预览」栏右侧，不再仅限周五
- **放纵餐**：周六/周日午餐自动替换为外出就餐卡片
- **菜品锁定**：每道菜可锁定/解锁，锁定后金色标识且不可换
